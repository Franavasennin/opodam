# OpoDAM v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir a OpoDAM acceso sin registro (sesión anónima Supabase), rediseño visual de academia, oposición Policía Local, secciones ProCoach AI y NUTRIPLAN, y música de concentración.

**Architecture:** SPA React 19 + TypeScript + Vite + TailwindCSS 3. El progreso se guarda en Supabase (tabla `progreso`) bajo un `user_id` de sesión anónima. El contenido de temas vive en `src/data/topics/<oposicion>/*.json` y se selecciona por slug.

**Tech Stack:** React 19, react-router-dom 7, @supabase/supabase-js 2, TailwindCSS 3, Vitest 4, pdf-parse 2 (extracción de PDFs).

**Spec:** `docs/superpowers/specs/2026-05-18-opodam-v2-design.md`

---

## FASE 1 — Acceso sin registro (sesión anónima)

### Task 1: Función de sesión anónima en supabase.ts

**Files:**
- Modify: `opodam/src/services/supabase.ts` (añadir función al final, antes de `cerrarSesion`)
- Test: `opodam/src/services/supabase.test.ts` (crear)

- [ ] **Step 1: Escribir el test que falla**

Crear `opodam/src/services/supabase.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { iniciarSesionAnonima } from './supabase'

describe('iniciarSesionAnonima', () => {
  it('es una función exportada', () => {
    expect(typeof iniciarSesionAnonima).toBe('function')
  })

  it('devuelve un objeto con campo error sin lanzar excepción', async () => {
    const res = await iniciarSesionAnonima()
    expect(res).toHaveProperty('error')
  })
})
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd opodam && npx vitest run src/services/supabase.test.ts`
Expected: FAIL — `iniciarSesionAnonima is not exported`.

- [ ] **Step 3: Implementar la función**

En `opodam/src/services/supabase.ts`, añadir tras `enviarMagicLink` (después de la línea 21):

```typescript
export async function iniciarSesionAnonima(): Promise<{ error: string | null }> {
  if (!supabase) return { error: 'Supabase no configurado' }
  // Reutilizar la sesión existente (anónima o con email) si la hay
  const { data: { session } } = await supabase.auth.getSession()
  if (session?.user) return { error: null }
  const { error } = await supabase.auth.signInAnonymously()
  if (error) console.error('[iniciarSesionAnonima] Supabase error:', error.message)
  return { error: error?.message ?? null }
}
```

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd opodam && npx vitest run src/services/supabase.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add opodam/src/services/supabase.ts opodam/src/services/supabase.test.ts
git commit -m "feat: sesion anonima de Supabase (iniciarSesionAnonima)"
```

---

### Task 2: Cablear sesión anónima y desconectar onboarding

**Files:**
- Modify: `opodam/src/App.tsx`
- Modify: `opodam/src/components/layout/RutaProtegida.tsx`

- [ ] **Step 1: Arrancar la sesión anónima en App.tsx**

En `opodam/src/App.tsx`, cambiar el import de servicios (línea 4-5) para añadir la nueva función:

```typescript
import { sincronizar } from './services/sync'
import { migrarProgresoLegado } from './services/storage'
import { iniciarSesionAnonima } from './services/supabase'
```

Reemplazar el `useEffect` del componente `App` (líneas 53-60) por:

```typescript
  useEffect(() => {
    iniciarSesionAnonima()
      .then(() => sincronizar())
      .catch(() => { /* sin conexión, ignorar */ })
    const onVisible = () => {
      if (document.visibilityState === 'visible') sincronizar().catch(() => {})
    }
    document.addEventListener('visibilitychange', onVisible)
    return () => document.removeEventListener('visibilitychange', onVisible)
  }, [])
```

- [ ] **Step 2: Desconectar las rutas de onboarding**

En `opodam/src/App.tsx`, sustituir el bloque de rutas de onboarding (líneas 68-71) por:

```jsx
        {/* AUTH DESACTIVADO — reactivar mas adelante.
            Rutas de onboarding y AuthCallback conservadas pero sin montar. */}
```

Los imports de `OnboardingEmail`, `OnboardingConfirmar`, `OnboardingOposicion` (líneas 24-26) se comentan con el prefijo `// AUTH DESACTIVADO:` para que el bundler no los incluya:

```typescript
// AUTH DESACTIVADO — reactivar mas adelante:
// import OnboardingEmail from './pages/onboarding/OnboardingEmail'
// import OnboardingConfirmar from './pages/onboarding/OnboardingConfirmar'
// import OnboardingOposicion from './pages/onboarding/OnboardingOposicion'
```

La ruta `/auth/callback` y el import de `AuthCallback` se mantienen activos (no estorban y `RootOrCallback` los usa).

- [ ] **Step 3: Convertir RutaProtegida en passthrough**

Reemplazar TODO el contenido de `opodam/src/components/layout/RutaProtegida.tsx` por:

```typescript
// src/components/layout/RutaProtegida.tsx
// AUTH DESACTIVADO — esta version deja pasar siempre.
// La logica de sesion/perfil original se reactivara mas adelante.
export function RutaProtegida({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

- [ ] **Step 4: Verificar compilación y arranque**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores de tipos.

Run: `cd opodam && npm run dev` y abrir `http://localhost:3000/`
Expected: la app redirige a `/mis-oposiciones` sin pasar por ninguna pantalla de email.

- [ ] **Step 5: Commit**

```bash
git add opodam/src/App.tsx opodam/src/components/layout/RutaProtegida.tsx
git commit -m "feat: acceso directo con sesion anonima, onboarding desconectado"
```

**Nota para el usuario:** activar *Authentication → Sign In / Providers → Anonymous sign-ins* en el panel de Supabase (proyecto `itmqjdupaquiggmmnyvf`). Sin ello la app funciona en modo solo-localStorage.

---

## FASE 2 — Rediseño visual (estilo academia)

### Task 3: Tokens de color corporativos en Tailwind

**Files:**
- Modify: `opodam/tailwind.config.js`

- [ ] **Step 1: Leer el config actual**

Run: `cd opodam && cat tailwind.config.js`
Expected: ver la sección `theme.extend`.

- [ ] **Step 2: Añadir la paleta de marca**

En `opodam/tailwind.config.js`, dentro de `theme.extend`, añadir la clave `colors` (sin borrar otras claves existentes):

```javascript
      colors: {
        marca: {
          900: '#1e3a8a',
          700: '#1d4ed8',
          600: '#2563eb',
          100: '#dbeafe',
          50:  '#eff6ff',
        },
        acento: {
          600: '#ea580c',
          100: '#ffedd5',
        },
      },
```

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add opodam/tailwind.config.js
git commit -m "style: paleta de marca corporativa en Tailwind"
```

---

### Task 4: Refinar componentes UI base

**Files:**
- Modify: `opodam/src/components/ui/Card.tsx`
- Modify: `opodam/src/components/ui/Badge.tsx`
- Modify: `opodam/src/components/ui/ProgressBar.tsx`

- [ ] **Step 1: Leer los tres componentes**

Run: `cd opodam && cat src/components/ui/Card.tsx src/components/ui/Badge.tsx src/components/ui/ProgressBar.tsx`
Expected: conocer sus props actuales para no romper consumidores.

- [ ] **Step 2: Actualizar estilos sin cambiar las props**

Para cada componente, conservar EXACTAMENTE la misma interfaz de props y solo ajustar las `className`:
- `Card`: contenedor `bg-white rounded-2xl border border-slate-200 shadow-sm`.
- `Badge`: `inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold` con color sobre `marca-100`/`marca-700`.
- `ProgressBar`: pista `bg-slate-200 rounded-full h-2`, relleno `bg-marca-600 rounded-full`.

No añadir ni quitar props. Si un componente recibe `className` extra por props, mantener ese passthrough.

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/components/ui
git commit -m "style: refinar Card, Badge y ProgressBar"
```

---

### Task 5: Rediseñar layout y navegación

**Files:**
- Modify: `opodam/src/components/layout/Layout.tsx`
- Modify: `opodam/src/components/layout/BottomNav.tsx`
- Modify: `opodam/src/components/layout/SideNav.tsx`

- [ ] **Step 1: Leer los tres ficheros de layout**

Run: `cd opodam && cat src/components/layout/Layout.tsx src/components/layout/BottomNav.tsx src/components/layout/SideNav.tsx`
Expected: conocer la estructura de navegación actual.

- [ ] **Step 2: Aplicar el nuevo lenguaje visual**

- `BottomNav`: fondo `bg-white border-t border-slate-200`, ítem activo `text-marca-600`, inactivo `text-slate-400`.
- `SideNav`: fondo `bg-marca-900 text-white`, ítem activo con `bg-white/10`.
- `Layout`: sin cambios estructurales salvo asegurar `bg-slate-50` en el `main`.

No cambiar las rutas ni los ítems de navegación, solo estilos.

- [ ] **Step 3: Verificar compilación y revisión visual**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.
Revisión: abrir la app en móvil y escritorio, comprobar navegación.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/components/layout
git commit -m "style: rediseno de layout y navegacion"
```

---

### Task 6: Rediseñar páginas principales

**Files:**
- Modify: `opodam/src/pages/MisOposiciones.tsx`
- Modify: `opodam/src/pages/OposicionDashboard.tsx`
- Modify: `opodam/src/pages/Temario.tsx`
- Modify: `opodam/src/pages/Tests.tsx`
- Modify: `opodam/src/pages/Estadisticas.tsx`

- [ ] **Step 1: Leer cada página antes de tocarla**

Run: `cd opodam && cat src/pages/MisOposiciones.tsx src/pages/OposicionDashboard.tsx src/pages/Temario.tsx src/pages/Tests.tsx src/pages/Estadisticas.tsx`
Expected: conocer la estructura JSX actual.

- [ ] **Step 2: Aplicar el lenguaje visual de academia**

Para cada página, conservar la lógica (hooks, estado, navegación) y solo reescribir el JSX/`className`:
- Cabeceras con marca `OpoDAM` y barra `bg-white border-b border-slate-200`.
- Secciones con título `text-slate-900 font-bold` y subtítulo `text-slate-500`.
- Tarjetas usando el componente `Card` y la paleta `marca-*`.
- Mantener mobile-first (`max-w-md mx-auto` donde ya existe).

No cambiar funciones, props ni rutas.

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/pages/MisOposiciones.tsx opodam/src/pages/OposicionDashboard.tsx opodam/src/pages/Temario.tsx opodam/src/pages/Tests.tsx opodam/src/pages/Estadisticas.tsx
git commit -m "style: rediseno de paginas principales estilo academia"
```

---

## FASE 3 — Música, ProCoach AI y NUTRIPLAN

### Task 7: Hook useMusica

**Files:**
- Create: `opodam/src/hooks/useMusica.ts`
- Test: `opodam/src/hooks/useMusica.test.ts`
- Create: `opodam/public/audio/concentracion.mp3` (pista lo-fi/ambiental libre de derechos)

- [ ] **Step 1: Añadir la pista de audio**

Colocar un fichero de música ambiental/lo-fi libre de derechos en `opodam/public/audio/concentracion.mp3`. Fuente recomendada: pista CC0 de pixabay.com/music o incompetech.com (licencia libre).

- [ ] **Step 2: Escribir el test que falla**

Crear `opodam/src/hooks/useMusica.test.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useMusica } from './useMusica'

describe('useMusica', () => {
  beforeEach(() => localStorage.clear())

  it('arranca desactivada por defecto', () => {
    const { result } = renderHook(() => useMusica())
    expect(result.current.activa).toBe(false)
  })

  it('alternar cambia el estado y lo persiste', () => {
    const { result } = renderHook(() => useMusica())
    act(() => result.current.alternar())
    expect(result.current.activa).toBe(true)
    expect(localStorage.getItem('opodam.musica')).toBe('on')
  })
})
```

- [ ] **Step 3: Ejecutar el test para verificar que falla**

Run: `cd opodam && npx vitest run src/hooks/useMusica.test.ts`
Expected: FAIL — `useMusica` no existe.

- [ ] **Step 4: Implementar el hook**

Crear `opodam/src/hooks/useMusica.ts`:

```typescript
import { useEffect, useRef, useState } from 'react'

const CLAVE = 'opodam.musica'

export function useMusica() {
  const [activa, setActiva] = useState(() => localStorage.getItem(CLAVE) === 'on')
  const audioRef = useRef<HTMLAudioElement | null>(null)

  useEffect(() => {
    if (!audioRef.current) {
      const audio = new Audio('/audio/concentracion.mp3')
      audio.loop = true
      audio.volume = 0.4
      audioRef.current = audio
    }
    const audio = audioRef.current
    if (activa) {
      audio.play().catch(() => { /* autoplay bloqueado hasta interaccion */ })
    } else {
      audio.pause()
    }
  }, [activa])

  function alternar() {
    setActiva(prev => {
      const siguiente = !prev
      localStorage.setItem(CLAVE, siguiente ? 'on' : 'off')
      return siguiente
    })
  }

  return { activa, alternar }
}
```

- [ ] **Step 5: Ejecutar el test para verificar que pasa**

Run: `cd opodam && npx vitest run src/hooks/useMusica.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 6: Commit**

```bash
git add opodam/src/hooks/useMusica.ts opodam/src/hooks/useMusica.test.ts opodam/public/audio/concentracion.mp3
git commit -m "feat: hook useMusica con persistencia de estado"
```

---

### Task 8: Botón flotante de música

**Files:**
- Create: `opodam/src/components/audio/BotonMusica.tsx`
- Modify: `opodam/src/components/layout/Layout.tsx`

- [ ] **Step 1: Crear el componente**

Crear `opodam/src/components/audio/BotonMusica.tsx`:

```typescript
import { useMusica } from '../../hooks/useMusica'

export function BotonMusica() {
  const { activa, alternar } = useMusica()
  return (
    <button
      onClick={alternar}
      aria-label={activa ? 'Desactivar musica' : 'Activar musica de concentracion'}
      title={activa ? 'Desactivar musica' : 'Activar musica de concentracion'}
      className={`fixed bottom-24 right-4 z-50 w-12 h-12 rounded-full shadow-lg flex items-center justify-center text-xl transition-colors ${
        activa ? 'bg-marca-600 text-white' : 'bg-white text-slate-500 border border-slate-200'
      }`}
    >
      {activa ? '♫' : '♪'}
    </button>
  )
}
```

- [ ] **Step 2: Montar el botón en Layout**

En `opodam/src/components/layout/Layout.tsx`, añadir el import y renderizar `<BotonMusica />` dentro del `div` raíz, después de `<BottomNav />`:

```typescript
import { BotonMusica } from '../audio/BotonMusica'
```

```jsx
      <BottomNav />
      <BotonMusica />
```

- [ ] **Step 3: Verificar compilación y comportamiento**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.
Revisión: el botón aparece flotante; al pulsar suena/para la música; al recargar recuerda el estado.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/components/audio/BotonMusica.tsx opodam/src/components/layout/Layout.tsx
git commit -m "feat: boton flotante de musica de concentracion"
```

---

### Task 9: Página ProCoach AI

**Files:**
- Create: `opodam/src/pages/ProCoachAI.tsx`
- Modify: `opodam/src/App.tsx` (añadir ruta `/procoach`)

- [ ] **Step 1: Crear la página**

Crear `opodam/src/pages/ProCoachAI.tsx`:

```typescript
import { useNavigate } from 'react-router-dom'

export default function ProCoachAI() {
  const navigate = useNavigate()
  return (
    <div className="min-h-screen bg-slate-50">
      <header className="bg-white border-b border-slate-200 px-4 py-3 flex items-center gap-3">
        <button onClick={() => navigate('/mis-oposiciones')} className="text-marca-600 text-sm">
          &larr; Inicio
        </button>
        <span className="font-bold text-slate-900">ProCoach AI</span>
      </header>
      <main className="max-w-md mx-auto p-4 space-y-4">
        <div className="rounded-2xl p-5 text-white bg-gradient-to-br from-marca-900 to-marca-600">
          <h1 className="font-extrabold text-xl">ProCoach AI</h1>
          <p className="text-sm opacity-90 mt-1">
            App movil con 1 agente IA especializado de disciplina deportiva: chat en
            tiempo real, gym mode, analisis de fotos, voz y planes personalizados.
            Backend Express + SQLite.
          </p>
        </div>

        <div className="bg-white rounded-2xl border border-slate-200 shadow-sm p-4">
          <p className="text-xs font-semibold text-slate-400 uppercase tracking-wide mb-2">Agente</p>
          <div className="flex items-center gap-3">
            <span className="text-xs font-bold px-2.5 py-0.5 rounded-full bg-marca-100 text-marca-700">DELTA</span>
            <div>
              <div className="text-sm font-semibold text-slate-900">Oposiciones fisicas</div>
              <div className="text-xs text-slate-500">Especialista GC, Policia, FF.AA. Espana</div>
            </div>
          </div>
        </div>

        <div className="bg-white rounded-2xl border border-slate-200 shadow-sm p-4">
          <p className="text-xs font-semibold text-slate-400 uppercase tracking-wide mb-2">Capacidades</p>
          <ul className="text-sm text-slate-700 space-y-1 list-disc pl-5">
            <li>Memoria activa (ultimos 20 mensajes)</li>
            <li>Modo gym (respuestas de 150 tokens o menos)</li>
            <li>Deteccion de problemas</li>
            <li>Limites profesionales</li>
          </ul>
        </div>

        <div className="bg-acento-100 rounded-2xl p-4">
          <p className="text-sm text-slate-700">
            <span className="font-semibold">Nicho de entrada:</span> oposiciones fisicas.
            Sin competencia directa en Espana y demanda constante.
          </p>
        </div>
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Registrar la ruta en App.tsx**

En `opodam/src/App.tsx`, añadir el import lazy junto a las demás páginas lazy (tras la línea 41):

```typescript
const ProCoachAI = lazy(() => import('./pages/ProCoachAI'))
```

Añadir la ruta dentro de `<Routes>`, antes del bloque de redirecciones:

```jsx
        <Route path="/procoach" element={<ProCoachAI />} />
```

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/pages/ProCoachAI.tsx opodam/src/App.tsx
git commit -m "feat: pagina promocional ProCoach AI"
```

---

### Task 10: Banner NUTRIPLAN y enlace a ProCoach en la home

**Files:**
- Create: `opodam/src/components/promo/BannerNutriplan.tsx`
- Modify: `opodam/src/pages/MisOposiciones.tsx`

- [ ] **Step 1: Crear el banner**

Crear `opodam/src/components/promo/BannerNutriplan.tsx`:

```typescript
export function BannerNutriplan() {
  return (
    <div className="mt-6 rounded-2xl p-5 bg-gradient-to-br from-emerald-600 to-emerald-800 text-white">
      <p className="text-xs font-semibold uppercase tracking-wide opacity-80">Tambien para ti</p>
      <h2 className="font-extrabold text-lg mt-1">NUTRIPLAN</h2>
      <p className="text-sm opacity-90 mt-1">
        Mejora tu fisico, tu peso y tu salud con planes de nutricion personalizados
        de la mano de la especialista en nutricion Ester Correa.
      </p>
    </div>
  )
}
```

- [ ] **Step 2: Integrar banner y enlace a ProCoach en la home**

En `opodam/src/pages/MisOposiciones.tsx`:
- Añadir el import: `import { BannerNutriplan } from '../components/promo/BannerNutriplan'`
- Tras el `<div className="space-y-3">...</div>` que mapea `misOposiciones` (cierre del bloque, dentro del `<main>`), añadir una tarjeta-enlace a ProCoach AI y el banner:

```jsx
        <button
          onClick={() => navigate('/procoach')}
          className="mt-4 w-full text-left rounded-2xl border border-slate-200 bg-white shadow-sm p-4"
        >
          <div className="text-sm font-semibold text-slate-900">ProCoach AI</div>
          <div className="text-xs text-slate-500">Entrenador IA para oposiciones fisicas</div>
        </button>

        <BannerNutriplan />
```

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/components/promo/BannerNutriplan.tsx opodam/src/pages/MisOposiciones.tsx
git commit -m "feat: banner NUTRIPLAN y acceso a ProCoach AI en la home"
```

---

## FASE 4 — Oposición Policía Local

### Task 11: Estructura de datos de Policía Local

**Files:**
- Create: `opodam/src/data/topics/policia-local/index.ts`
- Modify: `opodam/src/data/oposiciones.ts`

- [ ] **Step 1: Crear el index de Policía Local**

Crear `opodam/src/data/topics/policia-local/index.ts`. Los temas: ids 1-15 = bloque `general`, ids 16-37 = bloque `especifico`. El fichero declara `TEMAS_META` con 37 entradas (títulos provisionales, se sustituyen por los reales en Task 13), `TOTAL_TEMAS` y `cargarTema`:

```typescript
import type { Tema } from '../../../types'

export const TEMAS_META = [
  // Bloque General — ids 1..15 (titulos definitivos en Task 13)
  { id: 1,  titulo: 'Policia Local — Tema General 1',  bloque: 'general' },
  { id: 2,  titulo: 'Policia Local — Tema General 2',  bloque: 'general' },
  { id: 3,  titulo: 'Policia Local — Tema General 3',  bloque: 'general' },
  { id: 4,  titulo: 'Policia Local — Tema General 4',  bloque: 'general' },
  { id: 5,  titulo: 'Policia Local — Tema General 5',  bloque: 'general' },
  { id: 6,  titulo: 'Policia Local — Tema General 6',  bloque: 'general' },
  { id: 7,  titulo: 'Policia Local — Tema General 7',  bloque: 'general' },
  { id: 8,  titulo: 'Policia Local — Tema General 8',  bloque: 'general' },
  { id: 9,  titulo: 'Policia Local — Tema General 9',  bloque: 'general' },
  { id: 10, titulo: 'Policia Local — Tema General 10', bloque: 'general' },
  { id: 11, titulo: 'Policia Local — Tema General 11', bloque: 'general' },
  { id: 12, titulo: 'Policia Local — Tema General 12', bloque: 'general' },
  { id: 13, titulo: 'Policia Local — Tema General 13', bloque: 'general' },
  { id: 14, titulo: 'Policia Local — Tema General 14', bloque: 'general' },
  { id: 15, titulo: 'Policia Local — Tema General 15', bloque: 'general' },
  // Bloque Especifico — ids 16..37
  { id: 16, titulo: 'Policia Local — Tema Especifico 1',  bloque: 'especifico' },
  { id: 17, titulo: 'Policia Local — Tema Especifico 2',  bloque: 'especifico' },
  { id: 18, titulo: 'Policia Local — Tema Especifico 3',  bloque: 'especifico' },
  { id: 19, titulo: 'Policia Local — Tema Especifico 4',  bloque: 'especifico' },
  { id: 20, titulo: 'Policia Local — Tema Especifico 5',  bloque: 'especifico' },
  { id: 21, titulo: 'Policia Local — Tema Especifico 6',  bloque: 'especifico' },
  { id: 22, titulo: 'Policia Local — Tema Especifico 7',  bloque: 'especifico' },
  { id: 23, titulo: 'Policia Local — Tema Especifico 8',  bloque: 'especifico' },
  { id: 24, titulo: 'Policia Local — Tema Especifico 9',  bloque: 'especifico' },
  { id: 25, titulo: 'Policia Local — Tema Especifico 10', bloque: 'especifico' },
  { id: 26, titulo: 'Policia Local — Tema Especifico 11', bloque: 'especifico' },
  { id: 27, titulo: 'Policia Local — Tema Especifico 12', bloque: 'especifico' },
  { id: 28, titulo: 'Policia Local — Tema Especifico 13', bloque: 'especifico' },
  { id: 29, titulo: 'Policia Local — Tema Especifico 14', bloque: 'especifico' },
  { id: 30, titulo: 'Policia Local — Tema Especifico 15', bloque: 'especifico' },
  { id: 31, titulo: 'Policia Local — Tema Especifico 16', bloque: 'especifico' },
  { id: 32, titulo: 'Policia Local — Tema Especifico 17', bloque: 'especifico' },
  { id: 33, titulo: 'Policia Local — Tema Especifico 18', bloque: 'especifico' },
  { id: 34, titulo: 'Policia Local — Tema Especifico 19', bloque: 'especifico' },
  { id: 35, titulo: 'Policia Local — Tema Especifico 20', bloque: 'especifico' },
  { id: 36, titulo: 'Policia Local — Tema Especifico 21', bloque: 'especifico' },
  { id: 37, titulo: 'Policia Local — Tema Especifico 22', bloque: 'especifico' },
] as const

export type TemaMeta = typeof TEMAS_META[number]
export const TOTAL_TEMAS = TEMAS_META.length // 37

const TEMA_FILE_MAP: Record<number, string> = {
  1: 'tema-01', 2: 'tema-02', 3: 'tema-03', 4: 'tema-04', 5: 'tema-05',
  6: 'tema-06', 7: 'tema-07', 8: 'tema-08', 9: 'tema-09', 10: 'tema-10',
  11: 'tema-11', 12: 'tema-12', 13: 'tema-13', 14: 'tema-14', 15: 'tema-15',
  16: 'tema-16', 17: 'tema-17', 18: 'tema-18', 19: 'tema-19', 20: 'tema-20',
  21: 'tema-21', 22: 'tema-22', 23: 'tema-23', 24: 'tema-24', 25: 'tema-25',
  26: 'tema-26', 27: 'tema-27', 28: 'tema-28', 29: 'tema-29', 30: 'tema-30',
  31: 'tema-31', 32: 'tema-32', 33: 'tema-33', 34: 'tema-34', 35: 'tema-35',
  36: 'tema-36', 37: 'tema-37',
}

export async function cargarTema(id: number): Promise<Tema> {
  const fileName = TEMA_FILE_MAP[id]
  if (!fileName) throw new Error(`Tema con id ${id} no encontrado`)
  const modulo = await import(`./${fileName}.json`)
  return modulo.default as Tema
}
```

- [ ] **Step 2: Registrar la oposición**

En `opodam/src/data/oposiciones.ts`, añadir al array `OPOSICIONES` tras la entrada `cgpc` (después de la línea 19):

```typescript
  {
    slug: 'policia-local',
    nombre: 'Policia Local',
    descripcion: 'Cuerpos de Policia Local',
    disponible: true,
    color: '#1d4ed8',
    numTemas: 37,
  },
```

- [ ] **Step 3: Verificar compilación**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores (los `.json` aún no existen pero `import()` es dinámico, no rompe `tsc`).

- [ ] **Step 4: Commit**

```bash
git add opodam/src/data/topics/policia-local/index.ts opodam/src/data/oposiciones.ts
git commit -m "feat: estructura de datos de la oposicion Policia Local"
```

---

### Task 12: Selector de topics por oposición

**Files:**
- Modify: `opodam/src/data/topics/index.ts`
- Test: `opodam/src/data/topics/index.test.ts` (crear)

- [ ] **Step 1: Escribir el test que falla**

Crear `opodam/src/data/topics/index.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { obtenerTopics } from './index'

describe('obtenerTopics', () => {
  it('devuelve el modulo de CGPC con 45 temas', () => {
    const t = obtenerTopics('cgpc')
    expect(t.TOTAL_TEMAS).toBe(45)
    expect(typeof t.cargarTema).toBe('function')
  })

  it('devuelve el modulo de Policia Local con 37 temas', () => {
    const t = obtenerTopics('policia-local')
    expect(t.TOTAL_TEMAS).toBe(37)
  })

  it('lanza error para un slug desconocido', () => {
    expect(() => obtenerTopics('inexistente')).toThrow()
  })
})
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd opodam && npx vitest run src/data/topics/index.test.ts`
Expected: FAIL — `obtenerTopics` no existe.

- [ ] **Step 3: Implementar el selector**

Reemplazar TODO el contenido de `opodam/src/data/topics/index.ts` por:

```typescript
import type { Tema } from '../../types'
import * as cgpc from './cgpc/index'
import * as policiaLocal from './policia-local/index'

export interface ModuloTopics {
  TEMAS_META: ReadonlyArray<{ id: number; titulo: string; bloque: string }>
  TOTAL_TEMAS: number
  cargarTema: (id: number) => Promise<Tema>
}

const MODULOS: Record<string, ModuloTopics> = {
  'cgpc': cgpc,
  'policia-local': policiaLocal,
}

export function obtenerTopics(slug: string): ModuloTopics {
  const modulo = MODULOS[slug]
  if (!modulo) throw new Error(`Oposicion sin topics: ${slug}`)
  return modulo
}

// Compatibilidad: re-export de CGPC para consumidores que aun no pasan slug.
export { TEMAS_META, TOTAL_TEMAS, cargarTema } from './cgpc/index'
export type { TemaMeta } from './cgpc/index'
```

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd opodam && npx vitest run src/data/topics/index.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add opodam/src/data/topics/index.ts opodam/src/data/topics/index.test.ts
git commit -m "feat: selector de topics por oposicion (obtenerTopics)"
```

---

### Task 13: Generar los 37 ficheros JSON de Policía Local

**Files:**
- Create: `opodam/src/data/topics/policia-local/tema-01.json` … `tema-37.json`
- Modify: `opodam/src/data/topics/policia-local/index.ts` (títulos reales en `TEMAS_META`)

**Origen:** PDFs en `E:\opodam\PLocal`. Mapeo de fichero a id:
- `PL-GENERAL-TEMA-01..15.pdf` → ids 1..15 (bloque `general`).
- `PL-ESPECIFICA-TEMA-01..07.pdf` → ids 16..22; `PL-ESPECIFICA-TEMA-09..23.pdf` → ids 23..37 (bloque `especifico`). El específico tema 08 no existe en origen y se omite del mapeo.

- [ ] **Step 1: Extraer el texto de un PDF**

Para cada PDF, extraer el texto con `pdf-parse` (ya instalado). Ejemplo para el primero:

```bash
cd opodam && node -e "import('pdf-parse').then(async m => { const fs=require('fs'); const d=await m.default(fs.readFileSync('E:/opodam/PLocal/PL-GENERAL-TEMA-01.pdf')); console.log(d.text.slice(0,4000)) })"
```

Expected: el texto del tema en consola.

- [ ] **Step 2: Generar el JSON del tema**

Para cada id, crear `opodam/src/data/topics/policia-local/tema-NN.json` con la estructura estándar de tema (idéntica a los temas de CGPC):

```json
{
  "id": 1,
  "titulo": "<titulo real del PDF>",
  "bloque": "general",
  "secciones": [ { "id": "s1-1", "titulo": "...", "contenido": "..." } ],
  "esquemas": [ { "id": "e1-1", "titulo": "...", "tipo": "mermaid", "contenido": "graph TD\n    A[X] --> B[Y]" } ],
  "mapaMental": { "nodos": [ { "id": "n1", "texto": "...", "nivel": 0, "x": 0, "y": 0 } ], "aristas": [ { "desde": "n1", "hasta": "n2" } ] },
  "flashcards": [ { "id": "fc1-01", "anverso": "...", "reverso": "..." } ],
  "preguntas": [ { "id": "p1-01", "enunciado": "...", "opciones": ["a","b","c","d"], "respuestaCorrecta": 0, "explicacion": "..." } ]
}
```

Cada tema: 5 secciones, 2 esquemas mermaid, mapa mental de 13 nodos (nivel 0/1/2 con coordenadas x/y radiales), 20 flashcards (`fcNN-01..20`), 8 preguntas (`pNN-01..08`, `respuestaCorrecta` 0-3). Tras crear cada fichero, validarlo con `node -e "JSON.parse(require('fs').readFileSync('<ruta>','utf8'))"`.

- [ ] **Step 3: Actualizar los títulos en index.ts**

Sustituir los 37 títulos provisionales de `TEMAS_META` en `opodam/src/data/topics/policia-local/index.ts` por los títulos reales tomados de cada PDF. No cambiar los `id` ni el campo `bloque`.

- [ ] **Step 4: Validar los 37 ficheros**

Run:
```bash
cd "opodam/src/data/topics/policia-local" && for f in tema-*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || echo "INVALID: $f"; done; echo "Total: $(ls tema-*.json | wc -l)"
```
Expected: `Total: 37` y ningún `INVALID`.

- [ ] **Step 5: Commit (por fases, p. ej. cada 10 temas)**

```bash
git add opodam/src/data/topics/policia-local
git commit -m "content: temas de Policia Local"
```

---

### Task 14: Cablear las páginas para usar el slug de la oposición

**Files:**
- Modify: `opodam/src/pages/Temario.tsx`
- Modify: `opodam/src/pages/TemaDetalle.tsx`
- Modify: `opodam/src/pages/Tests.tsx`
- Modify: `opodam/src/pages/Simulacro.tsx`
- Modify: `opodam/src/pages/FlashcardsGlobal.tsx`
- Modify: `opodam/src/pages/Estadisticas.tsx`

- [ ] **Step 1: Localizar los imports de data/topics**

Run: `cd opodam && grep -rn "data/topics" src/pages`
Expected: lista de páginas y líneas que importan `TEMAS_META` / `TOTAL_TEMAS` / `cargarTema`.

- [ ] **Step 2: Sustituir el import fijo por el selector**

En cada página que aparezca en el grep, reemplazar el import directo de símbolos de `data/topics` por el uso de `obtenerTopics(slug)`. El `slug` se obtiene del router con `useParams<{ slug: string }>()` (todas estas páginas están bajo rutas `/oposicion/:slug/...`). Patrón:

```typescript
import { obtenerTopics } from '../data/topics'
import { useParams } from 'react-router-dom'

// dentro del componente:
const { slug } = useParams<{ slug: string }>()
const { TEMAS_META, TOTAL_TEMAS, cargarTema } = obtenerTopics(slug ?? 'cgpc')
```

Sustituir los usos de los símbolos antes importados por estas variables locales. No cambiar la lógica de cada página. Si una página ya usa `useParams` para `slug`, reutilizar esa variable.

- [ ] **Step 3: Verificar compilación y prueba funcional**

Run: `cd opodam && npx tsc -b --noEmit`
Expected: sin errores.
Prueba: entrar en la oposición `policia-local`, abrir el temario (37 temas), abrir un tema, hacer un test y ver estadísticas.

- [ ] **Step 4: Commit**

```bash
git add opodam/src/pages/Temario.tsx opodam/src/pages/TemaDetalle.tsx opodam/src/pages/Tests.tsx opodam/src/pages/Simulacro.tsx opodam/src/pages/FlashcardsGlobal.tsx opodam/src/pages/Estadisticas.tsx
git commit -m "feat: paginas de estudio multi-oposicion via obtenerTopics"
```

---

## Verificación final

- [ ] La app abre directamente en `/mis-oposiciones` sin pedir email.
- [ ] El progreso persiste y sincroniza con Supabase (sesión anónima).
- [ ] Rediseño visual aplicado y coherente en todas las pantallas.
- [ ] El botón de música activa/desactiva y recuerda el estado.
- [ ] ProCoach AI accesible desde la home; banner NUTRIPLAN visible al pie.
- [ ] La oposición Policía Local muestra 37 temas; temario, tests y estadísticas funcionan.
- [ ] `cd opodam && npx vitest run` — todos los tests pasan.
- [ ] `cd opodam && npm run build` — build sin errores.
